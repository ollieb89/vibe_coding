---
milestone: v1.1
audited: 2026-02-23T00:00:00Z
status: gaps_found
scores:
  requirements: 5/5
  phases: 0/2
  integration: 5/5
  flows: 3/3
gaps:
  requirements: []
  phases_unverified:
    - phase: "05-extension-filtering"
      status: "unverified"
      reason: "No VERIFICATION.md created during execute-phase"
    - phase: "06-frontmatter-classification"
      status: "unverified"
      reason: "No VERIFICATION.md created during execute-phase"
  integration:
    - location: "cli.py:251"
      severity: "low"
      description: "_count_stale_files calls walk_source without extensions=source.extensions, causing stale-file counts in `corpus status` to over-report (includes extension-filtered files)"
      affected_requirements: ["CONF-07"]
      note: "Display-only issue — indexing itself is correct"
tech_debt:
  - phase: "06-frontmatter-classification"
    items:
      - "indexer.py:139 — use_llm_classification formal parameter on index_source() is dead code (CLI never passes it; method reads source.use_llm_classification directly)"
      - "indexer.py:374-375 — two consecutive bare pass statements in close()"
  - phase: "05-extension-filtering"
    items:
      - "cli.py:251 — walk_source called without extensions= in status command stale-file counter"
---

# v1.1 Search Quality — Milestone Audit

**Audited:** 2026-02-23
**Status:** ⚠ gaps_found — verification trail missing (code complete)
**Milestone Goal:** Eliminate noise from search results by controlling which files get indexed and improving construct classification accuracy via frontmatter signals.

---

## Summary

All 5 requirements are **implemented and wired** end-to-end. The integration checker confirmed all three E2E flows work correctly and all 192 tests pass. However, **neither phase has a VERIFICATION.md file**, which is a formal blocker. The REQUIREMENTS.md traceability table still shows all requirements as `[ ]` Pending, and no phase SUMMARY has YAML frontmatter with `requirements-completed`.

The gap is in the **verification documentation trail**, not the implementation.

---

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Description | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------------|-----------------|---------------------|-----------------|--------------|
| CONF-06 | User can specify extensions in corpus.toml per source | missing | missing | `[ ]` Pending | **partial** (verification gap) |
| CONF-07 | corpus index skips files not in allowlist | missing | missing | `[ ]` Pending | **partial** (verification gap) |
| CONF-08 | Default allowlist applies when extensions not configured | missing | missing | `[ ]` Pending | **partial** (verification gap) |
| CLASS-04 | Classifier reads YAML frontmatter type/component_type/tags | missing | missing | `[ ]` Pending | **partial** (verification gap) |
| CLASS-05 | Recognized frontmatter type → confidence ≥ 0.9 | missing | missing | `[ ]` Pending | **partial** (verification gap) |

**Note:** All requirements have strong code evidence (implementation verified by integration checker and test suite). The "partial" status reflects missing VERIFICATION.md and SUMMARY YAML frontmatter — not missing implementation.

### Orphaned Requirements

All 5 requirements are technically **orphaned** — present in REQUIREMENTS.md traceability table but absent from all phase VERIFICATION.md files (since no VERIFICATION.md files exist).

---

## Phase Verification Status

| Phase | VERIFICATION.md | Test Evidence | Status |
|-------|-----------------|---------------|--------|
| 05-extension-filtering | ❌ missing | 179 tests passed (SUMMARY 05-02) | **unverified** |
| 06-frontmatter-classification | ❌ missing | 192 tests passed (SUMMARY 06-02) | **unverified** |

Both phases are **blockers** per the audit process. Missing VERIFICATION.md files mean formal verification was not run during execute-phase.

---

## Integration Findings

### E2E Flows — All Passing

| Flow | Status | Details |
|------|--------|---------|
| Extension filtering pipeline | ✅ Complete | corpus.toml → SourceConfig.extensions → walk_source(extensions=) → files_removed → CLI warning |
| Frontmatter classification pipeline | ✅ Complete | markdown with `type: skill` → classify_by_frontmatter() → ClassificationResult(0.95) → LanceDB chunk with classification_source/confidence |
| Search construct filter | ✅ Complete | `corpus search --construct skill` → WHERE construct_type='skill' predicate; frontmatter-classified files at high confidence improve accuracy |

### Cross-Phase Wiring — All Requirements Wired

| REQ-ID | Integration Path | Status |
|--------|-----------------|--------|
| CONF-06 | corpus.toml → SourceConfig.extensions (schema.py:78) → indexer passes extensions=source.extensions to walk_source (indexer.py:168-173) | ✅ Wired |
| CONF-07 | walk_source filters by suffix (scanner.py) → unmatched files never enter loop → _delete_stale_chunks removes previously-indexed files → files_removed printed (cli.py:172-175) | ✅ Wired (⚠ display bug in `status` command) |
| CONF-08 | DEFAULT_EXTENSIONS used as SourceConfig.extensions default (schema.py:17-19, 78); excludes .sh, .html, .json, .lock, binary | ✅ Wired |
| CLASS-04 | classify_by_frontmatter() reads type/component_type/tags → first priority in classify_file() → classification_source/confidence stored in LanceDB → ensure_schema_v3() upgrades existing tables | ✅ Wired |
| CLASS-05 | classify_by_frontmatter() returns ClassificationResult(val, 0.95, "frontmatter") for type:/component_type: matches (classifier.py:87, 93) | ✅ Wired |

### Integration Issues

| Severity | Location | Issue | Requirement |
|----------|----------|-------|-------------|
| Low | cli.py:251 | `_count_stale_files` calls `walk_source` without `extensions=source.extensions`; stale count in `corpus status` includes extension-filtered files (display-only, indexing correct) | CONF-07 |
| Low | indexer.py:139 | `use_llm_classification` formal parameter on `index_source()` is dead code — CLI never passes it; method reads `source.use_llm_classification` directly | None |
| Cosmetic | indexer.py:374-375 | Two consecutive bare `pass` in `close()` | None |

---

## Tech Debt

**Phase 05-extension-filtering:** 1 item
- `cli.py:251` — `_count_stale_files` in `status` command omits `extensions=source.extensions` from `walk_source` call; stale-file counts are inaccurate when extension filtering is active

**Phase 06-frontmatter-classification:** 2 items
- `indexer.py:139` — dead `use_llm_classification` formal parameter on `index_source()`
- `indexer.py:374-375` — two consecutive bare `pass` statements in `close()`

**Total: 3 items across 2 phases**

---

## Other Notes

- Phase 5 SUMMARY files (05-01-SUMMARY.md, 05-02-SUMMARY.md) are **untracked** in git — never committed
- Phase 5 roadmap checkboxes for plans are `[ ]` (not checked off) despite work being complete
- SUMMARY files lack YAML frontmatter with `requirements-completed` field (plain markdown only)
- Double-deletion bug fixed: `_delete_stale_chunks` called exactly once (confirmed by integration checker)
- Schema migration safe: `ensure_schema_v3()` is idempotent and uses LanceDB `add_columns` in-place
